{
  "name": "fighting-chance-server",
  "version": "1.0.0",
  "private": true,
  "description": "Express API server for Fighting Chance Transitional Restoration referral submissions",
  "main": "server.js",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "check": "node --check server.js && node --check index.js && node --check routes/referral.routes.js && node --check controllers/referral.controller.js && node --check services/email.service.js && node --check services/validators/referral.validator.js && node --check middleware/rateLimit.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.2.0",
    "fighting-chance": "file:..",
    "helmet": "^7.1.0",
    "nodemailer": "^6.9.13"
  },
  "engines": {
    "node": ">=18"
  }
}
